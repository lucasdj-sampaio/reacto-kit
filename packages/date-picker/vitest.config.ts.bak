import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'jsdom',
    include: ['src/**/__tests__/**/*.test.*', 'src/**/__tests__/*.*'],
    exclude: ['**/.storybook/**', 'src/stories/**', '**/node_modules/**'],
    globals: true,
  },
});
